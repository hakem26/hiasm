version: '3.8'
services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./app:/var/www/html
    depends_on:
      - db
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: yshcvdau_hiasm
      MYSQL_USER: yshcvdau_hiasmadmin
      MYSQL_PASSWORD: H72j51300!
    volumes:
      - ./database.sql:/docker-entrypoint-initdb.d/backup.sql
      - db_data:/var/lib/mysql
volumes:
  db_data:
